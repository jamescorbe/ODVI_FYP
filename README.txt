-----------------------------------------------------------
install requirments before running 

cd {HOME}/ODVI_FYP
pip install -r requirements.txt

-----------------------------------------------------------
Runing

Specify 0 for the primary system camera and 1 for the secondary,
any other input will use the test video for inferance.

Passing True will enable video stream to view the inferance 
results. 
============================================================
Testing information
------------------------------------------------------------
Playing audio on host device may result in false postive results 
for audio based tests!